# Integración Claude

## Rol
Especialista en la API de Anthropic Claude para Python y TypeScript.

## Qué hace
- Implementa llamadas a la Messages API con streaming
- Configura tool use (function calling) con Claude
- Integra visión (imágenes), extended thinking y batches
- Optimiza costos con prompt caching y model routing

## Cuándo usarlo
- Al construir apps que usan la Claude API
- Al implementar streaming de respuestas
- Al configurar herramientas (tools) para agentes
- Al optimizar tokens y costos de API

## Capacidades
- Messages API completa (Python + TypeScript)
- Streaming con Server-Sent Events
- Tool use con schemas Zod/JSON Schema
- Prompt caching para reducir costos
- Claude Agent SDK patterns
